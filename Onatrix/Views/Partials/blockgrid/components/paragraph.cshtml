@using Umbraco.Cms.Web.Common.PublishedModels
@inherits Umbraco.Cms.Web.Common.Views.UmbracoViewPage<Umbraco.Cms.Core.Models.Blocks.BlockGridItem>
@using ContentModels = Umbraco.Cms.Web.Common.PublishedModels
@using Umbraco.Cms.Core.PropertyEditors.ValueConverters

@{
    var paragraphContent = Model?.Content.Value<string>("paragraphContent");
    var textColor = Model?.Content.Value<ColorPickerValueConverter.PickedColor>("paragraphColor")?.Label.ToLower() ?? null;
    var textWeight = Model?.Content.Value<bool>("paragraphBold") == true ? "boldText" : "";
}

@if (!string.IsNullOrEmpty(paragraphContent)) 
{
    <p class="@textColor @textWeight">@paragraphContent</p> 
}